
	section	.data
		file	dq 	"images/pngMadeMini_size.txt",0

	section	.bss
		size	resq	0

	section	.text
		global	_start

_start:

	mov	rax, O_RDWR
	mov	rbx, file
	mov	rax, SYS_OPEN
acall:	syscall

	mov	rdx, 10
	mov	rcx, size
	mov	rbx, rax
	mov	rax, 0
bcall:	syscall

ccall:	mov	rax, size
dcall:	print size

	mov	rax, 60
	mov	rdi, 0
	syscall
